//DECLARACIÓN DE VARIABLES
let variableA = 3; //Variable que guarda el primer número.
let variableB = 4; //Variable que guarda el segundo número.
let suma = variableA + variableB; //Variable que guarda la suma de los dos anteriores.
/* A continuación se
   imprimirá por consola
   el resultado de la
   suma */
console.log("El resultado es: " + suma);