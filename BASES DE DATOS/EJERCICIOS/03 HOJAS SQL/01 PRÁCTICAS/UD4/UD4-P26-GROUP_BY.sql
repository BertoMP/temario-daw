--A)
SELECT AVG(SALARIO)
FROM EMPLE;

--B)
SELECT AVG(SALARIO)
FROM EMPLE
WHERE DEPT_NO = 10;

--C)
SELECT DEPT_NO, AVG(SALARIO)
FROM EMPLE
GROUP BY DEPT_NO
ORDER BY DEPT_NO;

SELECT DEPT_NO, AVG(SALARIO)
FROM EMPLE
WHERE DEPT_NO IS NOT NULL
GROUP BY DEPT_NO
ORDER BY DEPT_NO;